export interface Location {
  longitude: number;
  latitude: number;
}

export interface AedInfo {
  buildAddress: string;
  buildPlace: string;
  clerkTel: string;
  org: string;
  wgs84Lat: number;
  wgs84Lon: number;
  rnum: number;
}
